<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.devas.lab2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/movieapp"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activity_Task2"
            android:label="Second task"
            android:theme="@style/CustomTheme1"></activity>

        <activity android:name=".Activity_Task3"
            android:label="Third task"
            android:theme="@style/CustomTheme2"></activity>

        <activity android:name=".Activity_Task5"
            android:label="Fifth task"
            android:id="@+id/activity_Task5"
            android:theme="@style/CustomTheme3"
            ></activity>
    </application>

</manifest>